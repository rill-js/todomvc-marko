$ const { todos, filter } = out.global;

<section class="todoapp">
  <Header />
  <TodoList if(todos.length) filter=filter todos=todos />
</section>

<Footer>
  <h3>Go ahead, disable JavaScript.</h3>
  <p>Single-click to edit a todo</p>
</Footer>
